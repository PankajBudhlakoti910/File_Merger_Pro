{
  "buildCommand": "pip install --upgrade pip && pip install -r requirements.txt",
  "outputDirectory": ".",
  "framework": "python",
  "env": {
    "PYTHONUNBUFFERED": "1",
    "PIP_DISABLE_PIP_VERSION_CHECK": "1"
  }
}
